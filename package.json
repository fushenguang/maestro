{
  "name": "maestro",
  "private": true,
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:app": "pnpm --filter @maestro/desktop dev:app",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.14"
  }
}
