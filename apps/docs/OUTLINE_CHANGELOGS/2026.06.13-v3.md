# OUTLINE CHANGELOG — 2026.06.13 v3

> v3 重写：受众锁定 + 调研绑定 + 事实修正。
> 基线：2026.06.13 v2（23 章 use-centric 大纲）
> 触发：sunny 审阅 00-04 初稿后的反馈
> 讨论记录：`OUTLINE_DISCUSSIONS/2026.06.13-v3-rewrite.md`

---

## 核心变更（v2 → v3）

| 维度 | v2 | v3 |
|---|---|---|
| 受众 | 4 条路径隐含 4 种人（未锁定）| 锁定主受众：会写代码、会用终端、不熟悉 AI 编程工具的技术人 |
| 写作视角 | 游移（有时对新手、有时对专家）| 统一：写给懂代码但没让 AI 写过代码的人 |
| 调研使用 | 未绑定 | 每章 frontmatter 声明 `research_sources`，正文引用调研洞察 |
| 安装方式 | npm（已废弃）| Native Install（官方推荐）+ Homebrew 备选 |
| 模型代理 | 手动配 settings.json | 优先推荐 cc-switch，手动配置折叠 |
| 04 章排行榜 | Supabase | localStorage（纯前端，零新概念）|
| 02 章 `/prompt` | 存在（错误）| 移除——v2.1.174 中不存在 |
| AI 味 | 00 篇严重（教育哲学开场）| 砍掉元分析，直接声明受众和路径 |
| 每章 frontmatter | title + description | + `audience` + `cc_version` + `research_sources` |

## 逐章变更

### 00. 教程地图
- 砍掉"为什么大多数教程你读不完"整段
- 新增：受众声明（本教程写给谁、需要什么基础）
- 4 条路径精简为每条 2-3 句
- 去掉虚构第二人称

### 01. 安装配置
- 安装方式：npm → Native Install (`curl -fsSL https://claude.ai/install.sh | bash`)
- 新增 Homebrew 备选 + M1 Mac 真实踩坑经历
- 新增 CC 缩写提前声明
- 新增 cc-switch 推荐（模型代理优先方案）
- 终端手动配置折叠为可展开细节

### 02. 对话基础
- 删除 `/prompt` 全部引用（命令不存在）
- "旋钮 2"改写为：权限弹窗 + 精确 prompt 控制行为边界
- 新增 frontmatter：`cc_version: "2.1.174"` + `audience: "🟡"`

### 03. 贪吃蛇（上）
- 保留项目驱动结构
- 新增调研绑定：superpowers（Skill 写作方法论）+ anth-agent-skills（官方规范）
- 新增 frontmatter：`cc_version` + `audience` + `research_sources`

### 04. 贪吃蛇（下）
- Supabase → localStorage（纯前端排行榜）
- Memory 部分深度绑定 anth-memory（4 级加载链 + 子目录懒加载）
- Hooks 部分深度绑定 anth-hooks（5 类 handler + 3 层事件 + PreToolUse 决策字段）
- 新增 frontmatter：`cc_version` + `audience` + `research_sources`

## 新增规范（后续章节遵守）

1. **受众 flag**：🟢 零基础 / 🟡 需要编程基础 / 🔴 需要特定技术栈
2. **CC 版本声明**：每章 frontmatter 标注编写时的 CC 版本
3. **调研绑定**：写前列源 → 写中引用 → 写后记录
4. **真实锚点**：每章至少 1 个真实踩坑经历
5. **事实验证**：所有命令在实际环境中验证
