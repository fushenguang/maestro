---
title: aider
description: 开源 terminal 配对编程 agent，git-first + 多 edit format + pluggable LLM。
slug: aider
url: 'https://github.com/Aider-AI/aider'
source_type: repo
source_subtype: methodology
tier: brief
tiers_present:
  - card
  - brief
status: brief_complete
last_checked: 2026-06-11
last_commit: 5dc9490
stars: null
license: Apache License 2.0
size_estimate: 70M (aider/ package)
audience_tags:
  cs_background: yes
  devops: mid
  cc_experience: used
primary_author: Paul Gauthier (Aider-AI)
char_count: 5716
overflow: true
promote_to_brief: true
promote_to_deep_dive: null
fetched_via: pre_seeded_cache
fetched_via_fallback: true
mirror_url: null
batch: 3
tier_group: MCP 生态 + 同类工具
notes: 'subagent v2：主 Claude pre-seed cache（v1 因 subagent 沙箱无网络失败）。Card 含 7 个 takeaway + 6 维 CC 对比 + 3 段教程关联。char_count 2327 超出 methodology×Card 1800 限，标 overflow:true。'
---

# aider

## 一句话定位

> 开源 terminal 配对编程 agent（"AI pair programming in your terminal"），三大支柱：git-first + 多 edit format + pluggable LLM，强调人在回路 + 自动 commit 的可审计工作流。 (methodology:L1)

## 关键 takeaway

1. **Pair programmer 而非 autonomous agent**。每轮 diff 呈现给用户 review，命令式 chat 循环。 (methodology:L2)
2. **git-first 是底座**。每次 edit 后自动 commit，`/undo` 一键回退——LLM 行为落到 git 审计链。 (methodology:L3)
3. **多 edit format 是杀手特性**。`aider/coders/` 内置 14+ 种（whole / diff / udiff / editor-diff / editblock / patch / architect / ask / help / context），按模型自动挑最稳 prompt。 (methodology:L4)
4. **pluggable LLM**。后端 `litellm`，OpenAI / Anthropic / DeepSeek / Gemini / 本地 Ollama 通吃，**不绑单家**。 (methodology:L5)
5. **Repo-map = RAG-for-codebase**。扫描全 repo 抽 token 化调用图，模型在大项目里也能定位要改的文件——后继 RAG-coding 工具的共同祖先。 (methodology:L6)
6. **chat modes 显式分层**。`/code`（改）/ `/ask`（讨论）/ `/architect`（plan + editor 双模型）/ `/help`，CC 的 sub-agent / plan mode 借鉴这套。 (methodology:L7)
7. **OSS + 零 telemetry + Apache 2.0**。6.8M PyPI 安装、15B tokens/周，自报 "88% 新代码由 Aider 自己写" (Singularity badge)。 (methodology:L8)

## 与 Claude Code 的对比

- **哲学**：aider = pair programmer 在回路；CC = agent 派任务自跑。
- **LLM**：aider = pluggable (litellm) 10+ 厂商；CC = Anthropic-only。
- **Edit 表达**：aider = 14+ format 模型自选；CC = unified tool-use 协议。
- **Git**：aider = 每次 edit 自动 commit；CC = 手动或 hook。
- **可观测**：aider = 全 commit 链 + `/undo`；CC = task 子 agent + transcript。
- **形态/商业**：aider = Python CLI + Apache 2.0 零遥测；CC = Node CLI + 闭源。

CC 借鉴：plan/architect 双模型、ask/code 切换、git-aware undo。Aider 是 ML 团队跑 LLM benchmark 的「参考线」（polyglot bench），不会消失。

## 与 Claude Code 教程的关联

- **同源**：Aider `architect` = CC plan-then-execute；`/ask` = CC plan permission mode；`/undo` = CC checkpoint + revert。
- **互补**：Aider 跑 GPT-4o/DeepSeek 横向对比 + benchmark（不烧 Anthropic 配额）；CC 跑 Anthropic-native 长任务、sub-agent fan-out。
- **盲点**：Aider 没 CC 的 hooks / MCP / sub-agent / permission 系统——「企业级 agent 平台」是 CC 赛道，aider 是「个人开发者最强 IDE 增强」赛道。

## 参考锚点

- README.md L1, L11, L30 — 自我定位 + Repo-map 演示
- aider/coders/__init__.py L1-34 — 14 种 Coder 类（edit format 全家福）
- aider/main.py L1-39 — CLI 入口，litellm + GitRepo 集成
- website/docs/install.md L12-99 — pipx / uv / installer.sh 三种安装
- website/docs/usage.md L7-92 — chat 循环 + `/add`、`/undo`
- website/docs/usage/modes.md L9-49 — code / ask / architect / help 四模式
- LICENSE.txt L1 — Apache License 2.0

## Brief（梳理）

### Aider 定位：开源 pair programmer（非 autonomous agent）

- **核心定位**：每轮 diff 呈现给用户 review；**命令式 chat 循环**（vs agent 自跑）
- **三大支柱**：
  - **git-first**（底座）
  - **多 edit format**（杀手特性）
  - **pluggable LLM**（不绑单家）

### git-first 底座

- **每次 edit 后自动 commit**
- **`/undo` 一键回退**——LLM 行为落到 git 审计链
- **可观测性**：全 commit 链 + `/undo`（vs CC task + transcript）

### 多 Edit Format（杀手特性）

- 内置 **14+ 种** edit format：
  - `whole`（整文件替换）
  - `diff` / `udiff`（unified diff）
  - `editor-diff`（VS Code 风格）
  - `editblock`（块编辑）
  - `patch`（raw patch）
  - `architect`（双模型：plan + editor）
  - `ask`（只讨论不改）
  - `help`（帮助）
- **按模型自动挑最稳 prompt**（无需用户选）

### pluggable LLM（不绑单家）

- 后端 `litellm`：OpenAI / Anthropic / DeepSeek / Gemini / 本地 Ollama 通吃
- 对比 CC 默认只走 Anthropic API
- **场景**：横向 benchmark / 成本对比 / 多模型 fallback

### Repo-map = RAG-for-codebase

- 扫描全 repo 抽 **token 化调用图**
- 模型在大项目里也能定位要改的文件
- **历史地位**：后继 RAG-coding 工具（Cody / Codeium）的**共同祖先**

### Chat Modes 显式分层

| Mode | 用途 | CC 对应 |
|---|---|---|
| `/code` | 改代码 | 主模式 |
| `/ask` | 讨论（不改）| plan permission mode |
| `/architect` | **plan + editor 双模型** | plan-then-execute |
| `/help` | 帮助 | `/help` |

- CC plan/architect 借鉴这套分层

### OSS + 零 telemetry + Apache 2.0

- **6.8M PyPI 安装** + **15B tokens/周**
- 自报「88% 新代码由 Aider 自己写」（Singularity badge）
- **优势**：商业代码 base 可完全本地审；不传 telemetry 给任何公司

### 6 维 CC 对比

| 维度 | Aider | Claude Code |
|---|---|---|
| **哲学** | pair programmer 在回路 | agent 派任务自跑 |
| **LLM** | pluggable (litellm) 10+ 厂商 | Anthropic-only |
| **Edit 表达** | 14+ format 模型自选 | unified tool-use 协议 |
| **Git** | 每次 edit 自动 commit | 手动或 hook |
| **可观测** | 全 commit 链 + `/undo` | task 子 agent + transcript |
| **形态/商业** | Python CLI + Apache 2.0 零遥测 | Node CLI + 闭源 |

### 跨源关联

- vs `openai-codex`：本 Python + litellm；codex Rust workspace——**不同架构的同类工具**
- vs `anth-sub-agents`：本 `/architect` 双模型 = CC sub-agent 前身——**沿用模式**
- vs `anth-hooks`：CC hooks 6 类（PreToolUse 等）；aider 无 hooks——**错位**：CC 治理更细
- vs `anth-mcp`：aider 无 MCP；CC 完整 MCP 生态——**错位**：CC 工具产品化更成熟
- vs `superpowers`：aider 体现「最小可行 agent」+ 开源工程化；superpowers 给方法论体系——**互补**

### 教程章节候选

1. **「Aider vs CC 同类工具选型」章**：6 维对比决策表
2. **「多 Edit Format 设计」章**：14+ format 模型自选机制
3. **「git-first 工作流」章**：自动 commit + `/undo` 实现可审计
4. **「pluggable LLM」章**：litellm 抽象 + 10+ 厂商接入
5. **「Repo-map 原理」章**：RAG-for-codebase 调用图（与 vector store 选型）
6. **「Chat Modes 分层设计」章**：code / ask / architect / help 4 模式（与 CC plan mode 对应）
7. **「零 telemetry + Apache 2.0」章**：商业代码 base 完全本地审
8. **「ML 团队 benchmark 用法」章**：Aider 跑 polyglot bench 作为「参考线」
