---
title: "01. Sub-agents 是什么 + Worktree 隔离"
description: "理解 Sub-agents 的本质——不是分工，是隔离。单个 context 装不下复杂任务时，让多个 Agent 同时干活。"
audience: "🟡 需要编程基础"
cc_version: "2.1.176"
research_sources:
  - anth-sub-agents
---

> 🚧 本章尚未撰写。

**配项目**：观察一个 sub-agent 的执行过程——用 `--verbose` 看 agent 的完整日志，理解它是怎么启动、执行、返回的。

## 基础篇 · 学会高效使用

## 计划覆盖

- Sub-agents 是什么：CC 的「分身」机制
- 什么时候用：并发任务 × 独立 context × 不同专业方向
- Worktree 隔离：两个 agent 改不同文件，互不踩踏
- 启动方式：`/agents` 命令 vs 配置触发
